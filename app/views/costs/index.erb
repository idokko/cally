<div class = "cost-index-wrapper">
    <div class = "container">
        <div class = "row">
            <div class = "col-md-offset-3 col-md-6">
                <h1>売上履歴</h1>
                <% @costs.each do |cost| %>
                    <%= cost.price %>
                    <%= I18n.l(cost.created_at, format: :short) %>
                <% end %>
            </div>
        </div>
    </div>
</div>